# 🎯 راهنمای سریع فارسی

## نصب و اجرا در 3 دقیقه

### 1️⃣ نصب وابستگی‌ها

```bash
cd /Users/sedmahdiyar/Desktop/Minila/frontend
npm install
```

**یا با yarn:**

```bash
yarn install
```

---

### 2️⃣ تنظیم Backend

**مطمئن شوید Backend در حال اجرا است:**

```bash
# در ترمینال جدید
cd /Users/sedmahdiyar/Desktop/Minila/backend
docker-compose up -d
```

**بررسی Backend:**

```bash
curl http://localhost:8000/health
# باید پاسخ: {"ok":true} بدهد
```

---

### 3️⃣ اجرای Frontend

```bash
cd /Users/sedmahdiyar/Desktop/Minila/frontend
npm run dev
```

**✅ وب سایت در `http://localhost:3000` آماده است!**

---

## 🧪 تست کامل سیستم

### مرحله 1: ثبت‌نام

1. مرورگر را باز کنید: http://localhost:3000/auth/signup
2. فرم را پر کنید:
   ```
   نام: علی
   نام خانوادگی: احمدی
   ایمیل: ali@test.com
   رمز عبور: password123
   تکرار رمز: password123
   ```
3. روی "ثبت‌نام" کلیک کنید
4. منتظر پیام موفقیت بمانید
5. خودکار به صفحه ورود منتقل می‌شوید

---

### مرحله 2: دریافت کد OTP

1. در صفحه ورود، ایمیل خود را وارد کنید: `ali@test.com`
2. روی "ارسال کد تایید" کلیک کنید
3. **به MailHog بروید**: http://localhost:8025
4. ایمیل جدید را باز کنید
5. کد 6 رقمی را کپی کنید

---

### مرحله 3: ورود

1. در صفحه ورود، کد 6 رقمی را وارد کنید
2. روی "تایید و ورود" کلیک کنید
3. به داشبورد هدایت می‌شوید ✅

---

## 🎨 نمای صفحات

### 1. صفحه ورود
![Login Page]
- پس‌زمینه گرادیانت با رنگ‌های آبی، بژ و خاکستری
- کارت مرکزی با سایه نرم
- فرم ساده و تمیز
- دو مرحله: ایمیل → OTP

### 2. صفحه ثبت‌نام
![Signup Page]
- فرم کامل با validation
- پیام‌های خطای واضح
- انیمیشن موفقیت
- انتقال خودکار

### 3. داشبورد
![Dashboard]
- Header با نام کاربر
- 3 کارت آماری
- دکمه‌های اکشن سریع
- اطلاعات کامل کاربر

---

## 🔍 ساختار URL ها

```
/                          → redirect به login/dashboard
/auth/login                → صفحه ورود
/auth/signup               → صفحه ثبت‌نام
/dashboard                 → داشبورد (نیاز به احراز هویت)
```

---

## 🛠️ ابزارهای توسعه

### مشاهده ایمیل‌های ارسالی (MailHog)
```
http://localhost:8025
```

### مستندات API Backend
```
http://localhost:8000/docs
```

### سلامت Backend
```
http://localhost:8000/health
```

---

## 🎨 طراحی و رنگ‌ها

### رنگ‌های اصلی استفاده شده:

**آبی (Primary)**
- `#00A8E8` - دکمه‌ها و لینک‌ها
- `#0098D9` - hover state
- `#007EA7` - active state

**بژ/طلایی (Sand)**
- `#E5C189` - accent color
- `#D4AF89` - secondary accent

**خاکستری (Neutral)**
- `#1A1A1A` تا `#FAFAFA` - متن و پس‌زمینه

### فونت
- `-apple-system` (San Francisco در macOS)
- `BlinkMacSystemFont`
- `Segoe UI`

---

## ⚡ دستورات مفید

### Development
```bash
npm run dev          # اجرا در حالت توسعه
npm run build        # ساخت برای production
npm run start        # اجرای نسخه production
npm run lint         # بررسی کد
```

### Clear Cache
```bash
rm -rf .next
rm -rf node_modules
npm install
```

---

## 🐛 رفع مشکلات

### ❌ خطا: Backend در دسترس نیست

**علت**: Backend اجرا نشده یا پورت اشتباه است.

**راه حل**:
```bash
# بررسی Backend
cd ../backend
docker-compose ps

# راه‌اندازی مجدد
docker-compose restart
```

---

### ❌ خطا: CORS

**علت**: Backend CORS را برای localhost:3000 تنظیم نکرده.

**راه حل**: در `backend/.env`:
```env
CORS_ORIGINS=["http://localhost:3000"]
```

سپس Backend را restart کنید.

---

### ❌ خطا: OTP دریافت نمی‌شود

**علت**: MailHog اجرا نشده است.

**راه حل**:
```bash
cd ../backend
docker-compose up -d mailhog
```

سپس ایمیل‌ها را در http://localhost:8025 ببینید.

---

### ❌ خطا: Dependency نصب نمی‌شود

**راه حل**:
```bash
# پاک کردن cache
rm -rf node_modules package-lock.json

# نصب مجدد
npm install
```

---

## 📊 معماری

```
┌─────────────┐
│   Browser   │
│ (React/Next)│
└─────┬───────┘
      │
      │ HTTP/REST
      │
┌─────▼───────┐
│   Backend   │
│  (FastAPI)  │
└─────┬───────┘
      │
      ├─────► PostgreSQL
      ├─────► Redis
      └─────► MailHog
```

---

## 📞 پشتیبانی

مشکلی دارید؟

1. [README.md](./README.md) را بخوانید
2. [SETUP.md](./SETUP.md) را ببینید
3. در GitHub issue ایجاد کنید

---

**✨ حالا می‌توانید شروع کنید!**

```bash
npm run dev
```

سپس مرورگر را باز کنید: http://localhost:3000

