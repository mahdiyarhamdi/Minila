# 🚀 راهنمای سریع راه‌اندازی

## گام 1: نصب Dependencies

```bash
cd frontend
npm install
```

## گام 2: تنظیم متغیرهای محیطی

```bash
# ساخت فایل .env.local
echo "NEXT_PUBLIC_API_URL=http://localhost:8000" > .env.local
```

## گام 3: اطمینان از اجرای Backend

مطمئن شوید Backend روی پورت 8000 در حال اجرا است:

```bash
# در پوشه backend
cd ../backend
docker-compose up -d
```

## گام 4: اجرای Frontend

```bash
# بازگشت به پوشه frontend
cd ../frontend
npm run dev
```

اپلیکیشن در `http://localhost:3000` اجرا خواهد شد.

---

## 🎯 تست صفحات

1. **صفحه ثبت‌نام**: http://localhost:3000/auth/signup
2. **صفحه ورود**: http://localhost:3000/auth/login
3. **داشبورد**: http://localhost:3000/dashboard (نیاز به لاگین)

---

## 🔑 فلوی تست

### ثبت‌نام و ورود

1. به `http://localhost:3000/auth/signup` بروید
2. فرم را پر کنید:
   - نام: علی
   - نام خانوادگی: احمدی
   - ایمیل: ali@example.com
   - رمز عبور: password123
3. روی "ثبت‌نام" کلیک کنید
4. به صفحه ورود هدایت می‌شوید
5. ایمیل خود را وارد کنید و "ارسال کد تایید" بزنید
6. کد OTP را از MailHog دریافت کنید: http://localhost:8025
7. کد را وارد کنید و "تایید و ورود" بزنید
8. به داشبورد هدایت می‌شوید

---

## 📱 اسکرین‌شات‌های مهم

### صفحه ورود
- فرم ایمیل با طراحی مینیمال
- گرادیانت پس‌زمینه با رنگ‌های پروژه
- کارت با سایه نرم

### تایید OTP
- اینپوت 6 رقمی مرکزی
- دکمه ارسال مجدد
- پیام‌های خطای واضح

### داشبورد
- Header با نام کاربر
- کارت‌های آماری
- دکمه‌های اکشن سریع

---

## ⚠️ مشکلات رایج

### خطای CORS
اطمینان حاصل کنید که در `backend/.env` این تنظیم وجود دارد:

```env
CORS_ORIGINS=["http://localhost:3000"]
```

### Backend در دسترس نیست
بررسی کنید که Docker containers در حال اجرا هستند:

```bash
cd backend
docker-compose ps
```

### پورت 3000 در حال استفاده است
از پورت دیگری استفاده کنید:

```bash
npm run dev -- -p 3001
```

---

## 🎨 سفارشی‌سازی

### تغییر رنگ‌های اصلی

فایل `tailwind.config.js` را ویرایش کنید:

```js
colors: {
  primary: {
    500: '#00A8E8',  // رنگ اصلی خود را اینجا قرار دهید
  }
}
```

### اضافه کردن صفحه جدید

```bash
# در پوشه src/app
mkdir new-page
touch new-page/page.tsx
```

---

**مشکلی پیش آمد؟**  
به [README.md](./README.md) مراجعه کنید یا issue ایجاد کنید.

