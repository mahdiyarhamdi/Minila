# 📝 راهنمای تایپوگرافی - IRANYekan

این فایل توضیح می‌دهد که چگونه فونت **IRANYekan** با رعایت اصول طراحی حرفه‌ای در پروژه پیاده‌سازی شده است.

---

## 🎨 فونت اصلی پروژه

**IRANYekan** - یک فونت فارسی مدرن و خوانا با 8 وزن مختلف

---

## ⚖️ وزن‌های موجود و کاربرد آن‌ها

### 1. **Thin (100)** - نازک
```css
font-weight: 100;
```
**کاربرد**: متن‌های تزئینی و خیلی سبک (کمتر استفاده می‌شود)

**مثال در پروژه**: -

---

### 2. **Light (300)** - سبک ✅
```css
font-weight: 300;
```
**کاربرد**: متن‌های کمکی، توضیحات، زیرنویس‌ها

**مثال‌ها در پروژه**:
- توضیحات زیر عنوان صفحات (مثلاً "پلتفرم هماهنگی مسافر و بار")
- Helper text در فرم‌ها
- متن‌های فوتر
- توضیحات کارت‌های آماری

```tsx
// در صفحه Login
<p className="text-neutral-600 font-light text-base">
  پلتفرم هماهنگی مسافر و بار
</p>

// در Input component
<p className="mt-1.5 text-sm text-neutral-500 font-light">
  {helperText}
</p>
```

---

### 3. **Regular (400)** - معمولی ✅
```css
font-weight: 400;
```
**کاربرد**: متن اصلی صفحه، بدنه متن، پاراگراف‌ها

**مثال‌ها در پروژه**:
- متن‌های ورودی (Input)
- متن اصلی پاراگراف‌ها
- محتوای کارت‌ها
- پیام‌های خطا

```tsx
// پیش‌فرض body
body {
  font-weight: 400; /* Regular برای متن اصلی */
}

// متن‌های معمولی
<p className="text-center text-sm text-neutral-600 font-normal">
  حساب کاربری ندارید؟
</p>
```

---

### 4. **Medium (500)** - متوسط ✅
```css
font-weight: 500;
```
**کاربرد**: دکمه‌ها، لیبل‌ها، نام کاربر، تأکید متوسط

**مثال‌ها در پروژه**:
- تمام دکمه‌ها
- لیبل‌های فرم (label)
- نام کاربر در header
- لینک‌های مهم

```tsx
// در Button component
'font-medium' // Medium (500) برای دکمه‌ها

// نام کاربر
<span className="text-sm text-neutral-600 font-medium">
  {user.first_name} {user.last_name}
</span>

// لیبل‌ها
<label className="block text-sm font-medium text-neutral-700">
  آدرس ایمیل
</label>
```

---

### 5. **SemiBold (600)** - نیمه‌بولد ✅
```css
font-weight: 600;
```
**کاربرد**: عنوان‌های کوچک، h3-h6، عنوان کارت‌ها

**مثال‌ها در پروژه**:
- عنوان‌های کارت‌های اکشن
- زیرعنوان‌های بخش‌ها

```tsx
// عنوان کارت‌های اکشن
<h4 className="font-semibold text-neutral-900 mb-1">
  ایجاد کارت جدید
</h4>

// در CSS global
h3 {
  @apply font-semibold;
  font-weight: 600;
}
```

---

### 6. **Bold (700)** - بولد ✅
```css
font-weight: 700;
```
**کاربرد**: عنوان‌های اصلی صفحات، h1-h2، تیترها

**مثال‌ها در پروژه**:
- عنوان اصلی کارت‌های اصلی ("ورود به حساب")
- تیتر بخش‌ها ("شروع سریع", "اطلاعات حساب")
- عنوان کارت‌های آماری

```tsx
// عنوان اصلی کارت
<h2 className="text-2xl font-bold text-neutral-900 mb-2">
  ورود به حساب
</h2>

// تیتر بخش
<h3 className="text-xl font-bold text-neutral-900 mb-4">
  شروع سریع
</h3>

// در CSS global
h1, h2 {
  @apply font-bold;
  font-weight: 700;
}
```

---

### 7. **ExtraBold (800)** - خیلی بولد ✅
```css
font-weight: 800;
```
**کاربرد**: عنوان‌های بزرگ، hero headings، اعداد آماری

**مثال‌ها در پروژه**:
- عنوان اصلی صفحه داشبورد ("خوش آمدید")
- اعداد بزرگ در کارت‌های آماری (0، 5، 10)

```tsx
// عنوان hero
<h2 className="text-3xl font-extrabold text-neutral-900 mb-2">
  خوش آمدید، {user.first_name}! 👋
</h2>

// اعداد آماری
<p className="text-3xl font-extrabold text-primary-600">
  0
</p>
```

---

### 8. **Black (900)** - سیاه/بسیار ضخیم ✅
```css
font-weight: 900;
```
**کاربرد**: لوگو، برند، تیترهای خیلی بزرگ

**مثال‌ها در پروژه**:
- لوگوی "Minila" در تمام صفحات
- برند اصلی سایت

```tsx
// لوگوی اصلی
<h1 className="text-5xl font-black text-neutral-900 mb-2">
  Minila
</h1>

// لوگو در header
<h1 className="text-2xl font-black text-neutral-900">
  Minila
</h1>
```

---

## 📊 سلسله مراتب بصری (Visual Hierarchy)

از پایین به بالا (سبک به سنگین):

```
Light (300)     →  متن‌های کمکی، توضیحات
    ↓
Regular (400)   →  متن اصلی، بدنه
    ↓
Medium (500)    →  دکمه‌ها، لیبل‌ها، تأکید
    ↓
SemiBold (600)  →  عنوان‌های کوچک
    ↓
Bold (700)      →  عنوان‌های اصلی، تیترها
    ↓
ExtraBold (800) →  عنوان‌های بزرگ، اعداد مهم
    ↓
Black (900)     →  لوگو، برند
```

---

## 🎯 نکات کلیدی طراحی

### 1. **کنتراست مناسب**
- فاصله بین وزن‌ها حداقل 200 باشد
- از Light (300) به Bold (700) = 4 مرحله = ✅ کنتراست خوب
- از Regular (400) به Medium (500) = 1 مرحله = ❌ کنتراست کم

### 2. **ثبات (Consistency)**
- همیشه از همان وزن برای عناصر مشابه استفاده کنید
- مثلاً: تمام دکمه‌ها = Medium (500)

### 3. **خوانایی (Readability)**
- متن اصلی: Regular (400) یا Light (300)
- عنوان‌ها: Bold (700) یا بالاتر
- اندازه فونت + وزن فونت = خوانایی

### 4. **تعادل**
- در یک صفحه، حداکثر از 4-5 وزن مختلف استفاده کنید
- مثال صفحه Login:
  ```
  Black (900)   → لوگو
  Bold (700)    → عنوان کارت
  Medium (500)  → لیبل‌ها، دکمه‌ها
  Regular (400) → متن‌های معمولی
  Light (300)   → توضیحات
  ```

---

## 💻 نحوه استفاده در کد

### روش 1: استفاده از کلاس‌های Tailwind

```tsx
<h1 className="font-black">لوگو</h1>         // 900
<h2 className="font-extrabold">تیتر</h2>     // 800
<h3 className="font-bold">عنوان</h3>         // 700
<h4 className="font-semibold">زیرعنوان</h4> // 600
<button className="font-medium">دکمه</button> // 500
<p className="font-normal">متن</p>           // 400
<span className="font-light">توضیح</span>   // 300
<small className="font-thin">خیلی کوچک</small> // 100
```

### روش 2: استفاده از CSS مستقیم

```css
.logo {
  font-family: 'IRANYekan';
  font-weight: 900;
}

.heading {
  font-family: 'IRANYekan';
  font-weight: 700;
}

.button {
  font-family: 'IRANYekan';
  font-weight: 500;
}
```

### روش 3: استفاده پیش‌فرض در HTML Elements

```tsx
// این عناصر به صورت خودکار وزن مناسب دارند
<h1>عنوان</h1>       // font-weight: 700 (Bold)
<h2>زیرعنوان</h2>    // font-weight: 700 (Bold)
<h3>بخش</h3>         // font-weight: 600 (SemiBold)
<button>کلیک</button> // font-weight: 500 (Medium)
<label>لیبل</label>   // font-weight: 500 (Medium)
<p>متن</p>           // font-weight: 400 (Regular)
<small>کوچک</small>  // font-weight: 300 (Light)
```

---

## 📁 فایل‌های مربوطه

- **فونت‌ها**: `frontend/public/fonts/*.woff`
- **تعریف فونت**: `frontend/src/app/globals.css`
- **پیکربندی Tailwind**: `frontend/tailwind.config.js`
- **استفاده در کامپوننت‌ها**:
  - `frontend/src/components/Button.tsx`
  - `frontend/src/components/Input.tsx`
  - `frontend/src/components/Card.tsx`
- **استفاده در صفحات**:
  - `frontend/src/app/auth/login/page.tsx`
  - `frontend/src/app/auth/signup/page.tsx`
  - `frontend/src/app/dashboard/page.tsx`

---

## 🎨 مثال عملی - صفحه Login

```tsx
// لوگو - Black (900)
<h1 className="text-5xl font-black text-neutral-900">
  Minila
</h1>

// توضیح لوگو - Light (300)
<p className="text-neutral-600 font-light text-base">
  پلتفرم هماهنگی مسافر و بار
</p>

// عنوان کارت - Bold (700)
<h2 className="text-2xl font-bold text-neutral-900">
  ورود به حساب
</h2>

// توضیحات - Light (300)
<p className="text-neutral-600 font-light">
  ایمیل خود را وارد کنید
</p>

// لیبل فرم - Medium (500) - خودکار از CSS
<label>آدرس ایمیل</label>

// دکمه - Medium (500) - از کامپوننت Button
<Button>ارسال کد تایید</Button>

// متن فوتر - Light (300)
<p className="text-sm text-neutral-500 font-light">
  با ورود به سیستم، شما قوانین را می‌پذیرید.
</p>

// لینک - Medium (500)
<a className="font-medium">قوانین و مقررات</a>
```

**نتیجه**: یک سلسله مراتب بصری واضح و حرفه‌ای! 🎯

---

## ✅ چک‌لیست طراح

هنگام افزودن محتوای جدید، این سوالات را بپرسید:

- [ ] آیا این عنصر اصلی است؟ → **Bold (700)** یا بالاتر
- [ ] آیا این یک دکمه یا لیبل است؟ → **Medium (500)**
- [ ] آیا این متن اصلی است؟ → **Regular (400)**
- [ ] آیا این توضیح کمکی است؟ → **Light (300)**
- [ ] آیا این لوگو یا برند است؟ → **Black (900)**
- [ ] آیا این عدد یا آمار مهم است؟ → **ExtraBold (800)**

---

## 🚀 نتیجه‌گیری

با رعایت این اصول، تایپوگرافی پروژه:
- ✅ **حرفه‌ای**: سلسله مراتب واضح
- ✅ **خوانا**: تفکیک مناسب بین عناصر
- ✅ **ثابت**: همه‌جا از یک قاعده پیروی می‌کند
- ✅ **زیبا**: ترکیب هماهنگ وزن‌های مختلف

---

**تاریخ ایجاد**: 2025-11-08  
**فونت**: IRANYekan  
**وزن‌های استفاده شده**: 7 وزن از 8 وزن موجود  
**وضعیت**: ✅ پیاده‌سازی کامل

